---
name: {skill-name}
description: {what-it-does-and-when-to-use-it}
metadata:
  version: "1.0.0"
---

# {Skill Title}

{One-sentence operational purpose.}

## Hard Rules

- {Only rules needed to prevent material failure or preserve required invariants.}

## Resource Loading

- Read `references/{topic}.md` when {specific condition}.
- Use `assets/{template}.md` when {specific output/template condition}.

## Workflow

1. {Resolve inputs/context.}
2. {Perform core procedure.}
3. {Validate result.}
4. {Stop/return with explicit completion state if useful.}

## Completion

{Define what counts as done and what does not.}

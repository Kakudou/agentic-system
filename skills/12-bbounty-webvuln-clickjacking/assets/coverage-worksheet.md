# Page, Action, and Frame-Policy Coverage Worksheet

Use one row per selected in-scope HTML page. Do not invoke actions to populate the action category.

| Page/final URL | Auth state label | Page purpose | Action category | Redirect notes | CSP `frame-ancestors` | `X-Frame-Options` | Documented embedding need | Status |
|---|---|---|---|---|---|---|---|---|
|  |  | informational / interactive | none / low-risk / sensitive / out-of-scope |  |  |  | yes / no / unknown | inventoried / confirm / stop |

Action category labels: `none` for static/read-only content; `low-risk` for non-sensitive preference or navigation choices; `sensitive` for security, consent, payment, permission, identity, or account changes; `out-of-scope` when authorization does not cover the route.
